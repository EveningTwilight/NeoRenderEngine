import SwiftUI
import RenderEngine

struct ContentView: View {
    var body: some View {
        SceneGraphDemoView()
    }
}

/*
struct ContentView: View {
    @StateObject private var viewModel = DemoViewModel()
...existing code...
*/
